version: 1
applications:
  - frontend:
      phases:
        build:
          commands: yarn build
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: would-you-rather/src
